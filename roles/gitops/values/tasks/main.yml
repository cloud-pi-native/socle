---
- name: Loop over services
  ansible.builtin.include_tasks: ./template.yml
  loop: "{{ envs | subelements('apps') }}"
  loop_control:
    label: "{{ item.0.name }}"
  vars:
    envs:
      - name: "{{ dsc_name }}"
        apps:
          - argocd_app: keycloak
          - argocd_app: vault

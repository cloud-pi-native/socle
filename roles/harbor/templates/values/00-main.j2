expose:
  imagePullPolicy: IfNotPresent
  type: ingress
  ingress:
    className: {{ dsc.ingress.className | default('') }}
    hosts:
      core: {{ harbor_domain }}
      notary: {{ dsc.harbor.subDomain }}-notary{{ root_domain }}
    notary:
      annotations:
{% for key, val in dsc.ingress.annotations.items() %}
        {{ key }}: {{ val }}
{% endfor %}
      labels:
{% for key, val in dsc.ingress.labels.items() %}
        {{ key }}: {{ val }}
{% endfor %}
    harbor:
      annotations:
{% for key, val in dsc.ingress.annotations.items() %}
        {{ key }}: {{ val }}
{% endfor %}
      labels:
{% for key, val in dsc.ingress.labels.items() %}
        {{ key }}: {{ val }}
{% endfor %}
externalURL: https://{{ harbor_domain }}
persistence:
  enabled: true
  resourcePolicy: keep
  persistentVolumeClaim:
    registry:
      size: "{{ dsc.harbor.registry.pvcSize }}"
    jobservice:
      jobLog:
        size: "{{ dsc.harbor.jobService.pvcJobLogSize }}"
    database:
      size: "{{ dsc.harbor.database.pvcSize }}"
    redis:
      size: "{{ dsc.harbor.redis.pvcSize }}"
    trivy:
      size: "{{ dsc.harbor.trivy.pvcSize }}"
harborAdminPassword: "{{ dsc.harbor.adminPassword }}"
nginx:
  replicas: "{{ dsc.harbor.nginx.replicas }}"
  serviceAccountName: harbor-sa
  image:
    repository: docker.io/goharbor/nginx-photon
portal:
  replicas: "{{ dsc.harbor.portal.replicas }}"
  serviceAccountName: harbor-sa
  image:
    repository: docker.io/goharbor/harbor-portal
core:
  replicas: "{{ dsc.harbor.core.replicas }}"
  serviceAccountName: harbor-sa
  image:
    repository: docker.io/goharbor/harbor-core
jobservice:
  replicas: "{{ dsc.harbor.jobService.replicas }}"
  serviceAccountName: harbor-sa
  image:
    repository: docker.io/goharbor/harbor-jobservice
registry:
  replicas: "{{ dsc.harbor.registry.replicas }}"
  serviceAccountName: harbor-sa
  registry:
    image:
      repository: docker.io/goharbor/registry-photon
  controller:
    image:
      repository: docker.io/goharbor/harbor-registryctl
trivy:
  replicas: "{{ dsc.harbor.trivy.replicas }}"
  serviceAccountName: harbor-sa
  image:
    repository: docker.io/goharbor/trivy-adapter-photon
database:
  internal:
    serviceAccountName: harbor-sa
    image:
      repository: docker.io/goharbor/harbor-db
{% if dsc.global.backup.type == 'velero' %}
  podAnnotations:
    pre.hook.backup.velero.io/command: '["/bin/bash", "-c", "pg_dump -Fc  -U postgres -d registry  > /var/lib/postgresql/data/app.dump"]'
{% endif %}
redis:
  internal:
    serviceAccountName: harbor-sa
    image:
      repository: docker.io/goharbor/redis-photon
exporter:
  replicas: "{{ dsc.harbor.redis.replicas }}"
  image:
    repository: docker.io/goharbor/harbor-exporter
  serviceAccountName: harbor-sa
{% if dsc.global.metrics.enabled %}
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
{% endif %}

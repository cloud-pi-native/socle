{% if use_private_registry %}
image:
  repository: "{{ dsc.global.registry }}/jetstack/cert-manager-controller"

webhook:
  image:
    repository: "{{ dsc.global.registry }}/jetstack/cert-manager-webhook"

cainjector:
  image:
    repository: "{{ dsc.global.registry }}/jetstack/cert-manager-cainjector"

acmesolver:
  image:
    repository: "{{ dsc.global.registry }}/jetstack/cert-manager-acmesolver"

startupapicheck:
  image:
    repository: "{{ dsc.global.registry }}/jetstack/cert-manager-startupapicheck"
{% endif %}

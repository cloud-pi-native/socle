apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ dsc_name }}-harbor
  namespace: {{ dsc.grafana.namespace }}
spec:
  datasources:
    - inputName: "DS_HARBOR-PROMETHEUS"
      datasourceName: "Prometheus"
  resyncPeriod: 30s
  instanceSelector:
    matchLabels:
      app: {{ grafana_label }}
  grafanaCom:
    id: 14075
    revision: 2
